
# ProspectPro Environment Template
# -----------------------------------------------------------------------------
# Reference only – hydrate real values via Vercel env pulls or CI/Codespaces secrets:
#   npm run env:pull            # writes `.env.local` (development)
#   npm run env:pull:preview    # writes `.env.preview`
#   npm run env:pull:production # writes `.env.production`
#
# Never commit real secrets. Provide non-sensitive placeholders when sharing.

# -----------------------------------------------------------------------------
# CI/Codespaces/Automation Secrets (NEVER hydrated into Vercel or Supabase)
# Set these as GitHub/Codespaces/CI secrets only:
VERCEL_TOKEN=vercel_token_placeholder
SUPABASE_ACCESS_TOKEN=supabase_access_token_placeholder
SUPABASE_PROJECT_REF=your-project-ref
GITHUB_PERSONAL_ACCESS_TOKEN=ghp_placeholder
GH_TOKEN=ghp_placeholder

# -----------------------------------------------------------------------------
# Supabase Project (Backend/Edge/Agents) - Set in Supabase dashboard or as secrets
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=sb_publishable_placeholder
SUPABASE_SERVICE_ROLE_KEY=sb_secret_placeholder
SUPABASE_SESSION_JWT=your-session-jwt-placeholder # For Deno/Supabase Edge tests only
SUPABASE_DB_URL=postgres://user:password@host:5432/dbname
DATABASE_URL=${SUPABASE_DB_URL}

# -----------------------------------------------------------------------------
# Vercel Project (Frontend) - Set in Vercel project environment variables
VITE_SUPABASE_URL=${SUPABASE_URL}
VITE_SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}
VITE_AUTH_REDIRECT_URL=https://prospectpro.appsmithery.co/
VITE_EDGE_FUNCTIONS_URL=${SUPABASE_URL}/functions/v1

# -----------------------------------------------------------------------------
# Highlight.io Observability
# Vercel (frontend):
VITE_HIGHLIGHT_PROJECT_ID=highlight_project_placeholder
VITE_HIGHLIGHT_SERVICE_NAME=prospect-pro-frontend
# Supabase Edge/agents (server):
HIGHLIGHT_BACKEND_API_KEY=highlight_backend_placeholder
HIGHLIGHT_LOGDRAIN_ENDPOINT=https://pub.highlight.run/vercel/v1/logs
HIGHLIGHT_LOGDRAIN_SECRET=highlight_logdrain_placeholder
HIGHLIGHT_OTLP_ENDPOINT=https://otel.highlight.io:4318

# -----------------------------------------------------------------------------
# External APIs
# Vercel (frontend):
VITE_GOOGLE_MAP_ID=google_map_placeholder
VITE_GOOGLE_MAPS_API_KEY=google_maps_placeholder
VITE_GOOGLE_PLACES_API_KEY=google_places_placeholder
# Supabase Edge/agents (server):
AGENT_GOOGLE_PLACES_API_KEY=google_places_placeholder
AGENT_HUNTER_IO_API_KEY=hunter_placeholder
AGENT_NEVERBOUNCE_API_KEY=neverbounce_placeholder
AGENT_FOURSQUARE_API_KEY=foursquare_placeholder

# -----------------------------------------------------------------------------
# Agent Automation Helpers (agents only, set as CI/Codespaces secrets)
AGENT_VERCEL_TEAM_SLUG=appsmithery
AGENT_SUPABASE_ACCESS_TOKEN=supabase_cli_token_placeholder
AGENT_SUPABASE_DEFAULT_JWT=jwt_placeholder

# -----------------------------------------------------------------------------
# Runtime Defaults
NODE_ENV=development
PORT=3100
ALLOW_DEGRADED_START=false
LOG_LEVEL=info
REQUEST_TIMEOUT=30000

# -----------------------------------------------------------------------------
# Keep this template in source control. Hydrate `.env.local` before running
# deploy scripts so managed secrets are available locally without committing them.

## --- Supabase (project dashboard, not Vercel) ---
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_PROJECT_REF=your-project-ref
SUPABASE_ANON_KEY=sb_publishable_placeholder
SUPABASE_SERVICE_ROLE_KEY=sb_secret_placeholder
SUPABASE_SESSION_JWT=your-session-jwt-placeholder # For Deno/Supabase Edge tests only
SUPABASE_DB_URL=postgres://user:password@host:5432/dbname
DATABASE_URL=${SUPABASE_DB_URL}

## --- GitHub & Automation (GitHub secrets only) ---
# Do not hydrate these into Vercel or Supabase
GITHUB_WEBHOOK_SECRET=replace_me
GITHUB_PERSONAL_ACCESS_TOKEN=ghp_placeholder

## --- Vercel deployment tokens (GitHub/CI only) ---
VERCEL_TOKEN=vercel_token_placeholder
VERCEL_ORG_ID=team_placeholder
VERCEL_PROJECT_ID=project_placeholder

## --- Frontend (Vercel project env, per environment) ---
VITE_SUPABASE_URL=${SUPABASE_URL}
VITE_SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}
VITE_AUTH_REDIRECT_URL=https://prospectpro.appsmithery.co/
VITE_EDGE_FUNCTIONS_URL=${SUPABASE_URL}/functions/v1

## --- Highlight.io observability ---
# Vercel (frontend):
VITE_HIGHLIGHT_PROJECT_ID=highlight_project_placeholder
VITE_HIGHLIGHT_SERVICE_NAME=prospect-pro-frontend
# Supabase Edge/agents (server):
HIGHLIGHT_BACKEND_API_KEY=highlight_backend_placeholder
HIGHLIGHT_LOGDRAIN_ENDPOINT=https://pub.highlight.run/vercel/v1/logs
HIGHLIGHT_LOGDRAIN_SECRET=highlight_logdrain_placeholder
HIGHLIGHT_OTLP_ENDPOINT=https://otel.highlight.io:4318

## --- External APIs ---
# Vercel (frontend):
VITE_GOOGLE_MAP_ID=google_map_placeholder
VITE_GOOGLE_MAPS_API_KEY=google_maps_placeholder
VITE_GOOGLE_PLACES_API_KEY=google_places_placeholder
# Supabase Edge/agents (server):
AGENT_GOOGLE_PLACES_API_KEY=google_places_placeholder
AGENT_HUNTER_IO_API_KEY=hunter_placeholder
AGENT_NEVERBOUNCE_API_KEY=neverbounce_placeholder
AGENT_FOURSQUARE_API_KEY=foursquare_placeholder

## --- Agent automation helpers (agents only) ---
AGENT_VERCEL_TEAM_SLUG=appsmithery
AGENT_SUPABASE_ACCESS_TOKEN=supabase_cli_token_placeholder
AGENT_SUPABASE_DEFAULT_JWT=jwt_placeholder

## --- Runtime defaults ---
NODE_ENV=development
PORT=3100
ALLOW_DEGRADED_START=false
LOG_LEVEL=info
REQUEST_TIMEOUT=30000

##
# Keep this template in source control. Hydrate `.env.local` before running
# deploy scripts so managed secrets are available locally without committing them.
