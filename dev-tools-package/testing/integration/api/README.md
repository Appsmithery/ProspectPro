# ProspectPro API Clients

This directory stores generated or hand-crafted API client templates referenced by the MCP tooling. Keep scripts and utilities here rather than the deprecated `app/modules` tree.
